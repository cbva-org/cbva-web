## Styling

For styling, we use tailwindcss v4.

For complex styles that depend on multiple variables, use [tailwind-variants](https://www.tailwind-variants.org/docs/introduction) to resolve differences

## Complex logic in rendering

For complicated render logic, prefer [ts-pattern](https://github.com/gvergnaud/ts-pattern) for pattern matching over nested ternaries.
